# PipeWire fixture recorder

Reserved for a future diagnostic-safe fixture recorder. It must redact PIDs and private media/process metadata by default and require explicit review before a fixture is committed.
